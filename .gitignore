node_modules/
dist/
.env
.DS_Store
.gstack/

# smoke-test venv (browser render guard)
.venv/
scripts/__pycache__/

# Symlink to ../.orbit so the independent-QA runner reads ONE config (--repo here,
# because this is where the code and its history live). Never committed.
.orbit/loop.config.json
.orbit-evidence/
.orbit/locks/
.orbit/activity.jsonl
.orbit/reviews/
